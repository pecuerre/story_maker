<% content_for :title, "Event types" %>

<h1>Event types</h1>

<%= render "shared/taxonomy_tree",
	nodes: @event_types,
	new_url: ->(parent = nil) { new_universe_event_type_path(parent_id: parent&.id) },
	create_url: universe_event_types_path(),
	edit_url: ->(event_type) { edit_universe_event_type_path(id: event_type) },
	update_url: ->(event_type) { universe_event_type_path(id: event_type) },
	delete_url: ->(event_type) { universe_event_type_path(id: event_type) },
	model_param: "event_type",
	modal_fields: event_type_taxonomy_fields(Current.universe.event_types.order(:position, :id)) %>
