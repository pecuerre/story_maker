<% content_for :title, "Edit #{@story.name}" %>

<%= render "shared/page_header",
  title: "Edit story",
  eyebrow: @story.name,
  description: "Update the name and description used for this story." %>

<%= render "form", story: @story %>
