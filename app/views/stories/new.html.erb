<% content_for :title, "New story" %>

<%= render "shared/page_header",
  title: "New story",
  eyebrow: Current.universe.name,
  description: "Create a story with its own plot and section structure inside this universe." %>

<%= render "form", story: @story %>
