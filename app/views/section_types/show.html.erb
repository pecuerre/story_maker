<p style="color: green"><%= notice %></p>

<%= render @section_type %>

<div>
  <%= link_to "Edit this section type", edit_story_section_type_path(story_slug: Current.story.slug, id: @section_type) %> |
  <%= link_to "Back to section types", story_section_types_path(story_slug: Current.story.slug) %>

  <%= button_to "Destroy this section type", story_section_type_path(story_slug: Current.story.slug, id: @section_type), method: :delete %>
</div>
