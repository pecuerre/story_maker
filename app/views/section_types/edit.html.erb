<% content_for :title, "Editing section type" %>

<h1>Editing section type</h1>

<%= render "form", section_type: @section_type %>

<br>

<div>
  <%= link_to "Show this section type", story_section_type_path(story_slug: Current.story.slug, id: @section_type) %> |
  <%= link_to "Back to section types", story_section_types_path(story_slug: Current.story.slug) %>
</div>
