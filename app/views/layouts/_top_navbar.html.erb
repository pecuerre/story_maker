    <!-- Fixed Top Navbar -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top">
      <div class="container-fluid">
        <a class="navbar-brand" href="#">Story Maker</a>
        <ul class="navbar-nav flex-row ms-2">
          <li class="nav-item">
            <%= link_to 'All Stories', stories_path, :class => 'nav-link' %>
          </li>
          <li class="nav-item">
            <%= link_to 'Taxonomies', taxonomies_path, :class => 'nav-link' %>
          </li>
          <% if @story %>
            <li class="nav-item">
              <%= link_to @story.name, story_path(@story), class: 'text-info nav-link px-2' %>
            </li>
          <% end %>
        </ul>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav ms-auto">
            <li class="nav-item"><a class="nav-link" href="#">Home</a></li>
            <li class="nav-item"><a class="nav-link" href="#">About</a></li>
          </ul>
        </div>
      </div>
    </nav>
