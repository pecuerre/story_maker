<% content_for :title, "Edit #{@universe.name}" %>

<%= render "shared/page_header",
  title: "Edit universe",
  eyebrow: @universe.name,
  description: "Update the name used to identify this universe." %>

<%= render "form", universe: @universe %>
